# ---------- Build output ----------
bin/
obj/
out/
publish/
*.binlog
artifacts/

# ---------- Visual Studio / Rider ----------
.vs/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates

# ---------- ReSharper ----------
_ReSharper*/
*.DotSettings.user

# ---------- NuGet ----------
packages/
*.nupkg
*.snupkg
.nuget/
project.lock.json
project.assets.json

# ---------- Test results ----------
TestResults/
*.trx
*.coverage
*.coveragexml

# ---------- OS ----------
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# ---------- Logs / temp ----------
*.log
*.tmp
*.bak
*.swp
*.cache
*.pdb

# ---------- Local config overrides ----------
*.local.json

# ---------- 벤더 카메라 XML — 절대 커밋하지 않는다 (재배포 금지 자산) ----------
# 테스트 픽스처는 자작 XML 만 쓴다. 실카메라에서 받은 XML 은 이 폴더에만 두고 저장소에 싣지 않는다.
_vendor-xml/
*.vendor.xml

# ---------- Claude Code 작업 데이터 ----------
# 폴더 전체 제외 — worktree·로컬 설정 등 이 PC 한정 작업 상태가 섞여 있어
# 공유하면 오히려 혼동을 부른다. 폴더는 repo 복사 시 동행하는 것으로 충분.
.claude/

# AI 코딩 도구용 지침 — 사람이 읽는 문서는 README.md 와 docs/ 가 원장이다.
CLAUDE.md

# Excel/Office 임시 잠금 파일
~$*
